## Доделываем 6 правок кабинета и попапа

### Уже сделано (до плана, требует только проверки)
1. **Попап при входе** — `WebinarEntryPopup` смонтирован в `src/app/(site)/layout.tsx` (раньше компонент существовал, но нигде не использовался — потому и не появлялся). Показ раз за сессию через sessionStorage, задержка 0.8с.
2. **Заголовок окна** — «Записаться на курс» → «Записаться на бесплатный вебинар» (`WebinarSignupPanel.tsx`).
3. **Левый сайдбар (JSX)** — сверху ссылка «НА ГЛАВНУЮ → district-school.by», внизу карточка-счётчик до ЦТ (иконка + число + дата). Стрелка «назад» у заголовка «Личный кабинет» удалена.
4. **Состояние профиля** — расширено `resultType: 'ct' | 'grade'` и `resultValue`; `displayName` теперь берётся из сохранённого в настройках имени (обновляется сразу во всём кабинете).

### Осталось сделать

**1. CSS новых элементов сайдбара** (`cabinet.css`):
- `.cab-nav-home` — пункт-ссылка: иконка дома, «На главную» + приглушённый домен, стрелка справа; hover-подсветка;
- `.cab-exam-countdown` — карточка: иконка цели, крупное число дней, подпись «До ЦТ по математике», дата 30 мая 2027; тонкая рамка, лёгкий cyan-акцент;
- удалить неиспользуемые `.cab-back`, `.cab-user-mini`, `.cab-avatar-sm`.

**2. «Текущий результат» в настройках** (`CabinetShell.tsx`):
- в форму редактирования профиля добавить блок: переключатель «Балл ЦЭ / Оценка по предмету» + числовое поле с валидацией (ЦЭ: 0–100, оценка: 0–10);
- сохранение в `profileSaved` (TEMP-локально, как класс/цель — комментарий про будущее поле в Supabase уже есть);
- в просмотре профиля добавить тег результата рядом с классом и целью.

**3. Правый профиль** (`CabinetShell.tsx` + CSS):
- заменить блок «В системе» → «Результат» со значением из профиля (`X баллов` или `X / 10`);
- цель больше не обрезается: у `.cab-stats strong` убрать nowrap/ellipsis для цели (разрешить перенос, меньший кегль), сетка stats — «Класс / Результат» в ряд, «Цель» отдельной широкой строкой.

**4. Кабинет групповых — симпатичнее** (полировка текущей золотой темы):
- герой следующего занятия: аккуратнее компоновка даты/темы (дата-плитка в тон, золотой кикер уже есть), лёгкий внутренний отступ и разделители между блоками инфо;
- мини-строка «Мини-группа №2 · 6 учеников» — компактнее, аватарки участников чуть меньше.

**5. Расписание — приятнее** (CSS-полировка):
- карточки занятий в сетке: чуть больше воздуха, скругление, hover-подъём;
- шапка дня «сегодня» — мягкая подсветка колонки (уже есть) + подчеркнуть дату;
- блок «Ближайшее» — выровнять композицию (крупное время, аккуратная плашка типа, заметка про Telegram уже есть);
- список «Предстоящие» — строки с чуть большим ритмом.

**6. Проверка**: `tsc --noEmit`, `eslint`, визуальная проверка в браузере на `localhost:3000/cabinet-preview` (сайдбар, профиль, настройки с результатом, групповые, расписание) + главная сайта с попапом.

Трогаем только `CabinetShell.tsx`, `cabinet.css`, уже изменённые `layout.tsx`/`WebinarSignupPanel.tsx`. Деплой не трогаем (SSH-проблема сервера — отдельно).